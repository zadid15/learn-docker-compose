# How to create container with Docker Compose
docker compose create
# How to run a container with Docker Compose
docker compose start
# How to preview a container with Docker Compose
docker compose ps
docker compose ps -a 
# How to stop a container with Docker Compose
docker compose stop
# How to remove a container with Docker Compose
docker compose down